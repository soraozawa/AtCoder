#include<bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    int N, ans=0;
    cin >> N;
    ll sx, sy, tx, ty;

    // 無向片を貼る
    // スタート地点、ゴール地点を判定
    // 辺で繋がっているかどうかを再帰的に判定

    cout << ans << endl;
} 
